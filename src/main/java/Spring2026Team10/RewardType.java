package Spring2026Team10;

/**
 * Defines all specific types of rewards.
 */
public enum RewardType {
    /** Grants the player 50 points and a temporary 1-second speed boost. */
    LAPTOP,
    /** Grants the player 75 points and a temporary 1-second speed boost. */
    STUDENT_ID,
    /** Grants the player 100 points and a temporary 1-second speed boost. */
    RACCOON
}
